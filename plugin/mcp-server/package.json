{
  "name": "@handoff/plugin-mcp-server",
  "private": true,
  "type": "module",
  "description": "Bundled, self-contained build of @handoff/mcp-server — generated by plugin/build.mjs, not hand-edited. Ships inside the plugin directory so the plugin has no dependency on the rest of the handoff monorepo."
}
